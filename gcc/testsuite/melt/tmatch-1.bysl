;; -*- lisp -*-
;; file  gcc/testsuite/melt/tmatch-1.bysl
;; test the match operation

(defun tmatch_aa (x)
  (match x
	 ( ?(tuple_nth 0 ?(instance class_symbol :named_name ?n))
	    n)
	 ( ?(instance class_named :named_name ?y)
	  y)
	 ( ?_ 
	  x)))

;; eof tmatch-1.bysl